services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sokowise
      POSTGRES_PASSWORD: sokowise
      POSTGRES_DB: sokowise
    ports:
      - "5437:5432"
    volumes:
      - sokowise_pgdata:/var/lib/postgresql/data

volumes:
  sokowise_pgdata:
