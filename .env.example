# Database (Docker Compose Postgres by default — see docker-compose.yml)
DATABASE_URL="postgresql://sokowise:sokowise@localhost:5437/sokowise"

# Auth.js
AUTH_SECRET=
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Password used for every seeded demo account (sellers, community
# contributors, admin) — see prisma/seed.ts and the README's demo credentials.
SEED_DEMO_PASSWORD="Demo1234!"
